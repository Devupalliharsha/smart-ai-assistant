# Core web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Database (SQLite — no installation needed)
sqlalchemy==2.0.30

# AI — Groq API client (free, fast Llama 3)
groq==0.9.0

# RAG — ChromaDB (local vector store, no server needed)
chromadb==0.5.0

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9

# PDF reading (optional — only needed if you upload PDFs)
pdfplumber==0.11.0

# Environment variables
python-dotenv==1.0.1